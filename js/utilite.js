export const addZero = n => n < 10 ? '0' + n : n;

// добавить 0 к таймеру


// через другую функцию

// const addZero = (n) => { 

    // другой вариант

    // return n < 10 ? '0' + n : n; 

    // еще один вариант
    // const newN = n < 10 ? '0' + n : n;

    // return newN;

//  второй вариант
    // if(n < 10) {
    //     return '0' + n;
    // }

    // return n;
// }